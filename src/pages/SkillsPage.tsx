/**
 * Lisa Skills Page
 * UI for managing skills (inspired by OpenClaw's ClawHub)
 */

import { SkillsPanel } from '../components/gateway/SkillsPanel';

export default function SkillsPage() {
  return <SkillsPanel />;
}
